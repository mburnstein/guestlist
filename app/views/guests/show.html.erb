
<h1><%= @guest.first_name %></h1>
<h1><%= @guest.last_name %></h1>

<%= link_to "Edit", edit_guest_path(@guest), class: 'btn btn-primary' %>
<br />
<%= link_to 'Destroy', guest_path(@guest), method: :delete, data: {confirm: 'Are you sure you want to delete this?'},class: 'btn btn-danger' %>

